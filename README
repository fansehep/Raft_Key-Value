# Raft KV Server
#### 本仓库是 MIT 6.824 的课后 lab
- raft 是一种分布式共识算法，即在分布式系统中，让各个服务器之间数据拥有一个不错的同步能力，raft只能对你的服务器集群提供一种最终一致性状态和一个不错的容灾能力，并且在剩余的节点 >= 1 / 2, 仍然可以继续向外提供服务。

### 2A 领导人选举 (Leader Election)
- 所有服务器节点启动时，都拥有一个任期，和一个号码，且所有服务器启动时都是 Follower, Raft 中每个节点拥有三个角色 Leader, Follower, Candidate, 且正确的集群状况下，智慧